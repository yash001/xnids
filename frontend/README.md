# xNIDS Console — Frontend

React (Vite) single-page app. See the project root `README.md` for full
setup, architecture (HLD/LLD), and screen-by-screen documentation.

Quick start:

```bash
npm install
cp .env.example .env   # set VITE_API_URL if backend isn't on localhost:8000
npm run dev
```
